# Test Notes

- `ME8_umariyya_with_daughter` documents the Maliki handling when a direct daughter is present alongside the Umariyya trio (spouse + parents). In this configuration the mother keeps her Qur'anic share of `1/6` because descendants exist, and the father absorbs the remainder as `asabah`. This matches the behaviour encoded in `RuleBookMaliki`.
