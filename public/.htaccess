# Desactiva el listado de directorios
Options -Indexes

# Bloquea acceso directo a carpetas internas si llegan a existir en el docroot
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^tools(/.*)?$ - [F,L]
    RewriteRule ^_legacy(/.*)?$ - [F,L]
</IfModule>
